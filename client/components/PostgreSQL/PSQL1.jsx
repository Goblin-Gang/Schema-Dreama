import React, { useState } from "react";
import { Link, Outlet } from "react-router-dom";

export default function PSQL1 (){
    return (
        <>
          <div>
            <input type='checkbox'>What is a PostgreSQL Database</input>
            <button>Complete</button>
          </div>
        </>
    );
}